//! Astra UI's public component facade.
//!
//! Component implementations live in [`crate::components`]. This module
//! keeps the original `crate::ui::*` import path stable for existing users.

pub use crate::components::*;
